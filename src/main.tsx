import React from "react";
import ReactDOM from "react-dom/client";
import PrombezTrainer from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PrombezTrainer />
  </React.StrictMode>
);
